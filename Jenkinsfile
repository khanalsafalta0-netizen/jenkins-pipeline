pipeline {
    agent any

    stages {
        stage('build') {
            steps {
                echo 'we are running is build stage'
            }
        }
        stage('test') {
            steps {
                echo 'we are running in test stage'
            } }
        stage('deploy') {
            steps {
                echo 'we are running in deploy stage'
            }
        }
    }
}
